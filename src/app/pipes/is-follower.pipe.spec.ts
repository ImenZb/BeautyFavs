import { IsFollowerPipe } from './is-follower.pipe';

describe('IsFollowerPipe', () => {
  it('create an instance', () => {
    const pipe = new IsFollowerPipe();
    expect(pipe).toBeTruthy();
  });
});
