import { LikesInitPipe } from './likes-init.pipe';

describe('LikesInitPipe', () => {
  it('create an instance', () => {
    const pipe = new LikesInitPipe();
    expect(pipe).toBeTruthy();
  });
});
